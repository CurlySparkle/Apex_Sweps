include('shared.lua')

function ENT:Draw()
    --if not self.Rotation then
        --self.Rotation = math.Rand(-180,180)
    --end
    
    --self.Rotation = self.Rotation + FrameTime()*1000
    --self:SetRenderAngles( Angle(self:GetAngles().x,self:GetAngles().y,self.Rotation) )
    
    --self:DrawModel()
end